<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.my_java_app.mapper.RoleMapper">

    <resultMap id="RoleResultMap" type="com.example.my_java_app.entity.RoleEntity">
        <id     property="id"          column="id"          />
        <result property="roleName"    column="role_name"   />
        <result property="description" column="description" />
    </resultMap>

    <!--
        Lấy roles của user bằng cách JOIN qua bảng user_roles.
        user_roles.user_sub → cognito_sub của user.
    -->
    <select id="findRolesByUserSub" parameterType="string" resultMap="RoleResultMap">
        SELECT
            r.id,
            r.role_name,
            r.description
        FROM roles r
        INNER JOIN user_roles ur ON ur.role_id = r.id
        WHERE ur.user_sub = #{cognitoSub}
          AND r.is_deleted = 0
    </select>

</mapper>
