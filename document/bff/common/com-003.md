# COM-003 — POST /com-003 — (Deprecated / Stubbed)

## Trạng thái: ❌ KHÔNG SỬ DỤNG

Auth0 không có `NEW_PASSWORD_REQUIRED` challenge. Endpoint này trả `410 Gone`.

## Process Flow

```
Frontend POST /com-003
  → BFF trả 410 Gone
  → { processStatus: 0, message: ['This endpoint is not supported with Auth0'] }
```

---

## Lý do

Cognito có flow đặt mật khẩu lần đầu (`NEW_PASSWORD_REQUIRED`) dành cho admin-created users.
Auth0 không có flow tương đương — passwords được set trực tiếp trong Auth0 dashboard.

---

## BFF Endpoint

- **Method**: POST
- **Path**: `/api/com-003`
- **Response**: `410 Gone`
